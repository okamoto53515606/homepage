# GEMINI_API_KEY
GEMINI_API_KEY=""

# 【ローカル開発時のみ利用/本番はSecret Manager】Google OAuth クライアントID
NEXT_PUBLIC_GOOGLE_CLIENT_ID=""

# 【ローカル開発時のみ利用/本番はSecret Manager】stripe API 公開可能キー（クライアントサイドで使用）
NEXT_PUBLIC_STRIPE_PUBLIC_KEY=""

# 【ローカル開発時のみ利用/本番はSecret Manager】stripe API シークレットキー（サーバーサイドのみ）
STRIPE_SECRET_KEY="sk_test_dummy"

# 【ローカル開発時のみ利用/本番はSecret Manager】stripe 署名シークレット
STRIPE_WEBHOOK_SECRET="whsec_dummy"

# 【ローカル開発時のみ利用/本番はSecret Manager】stripe > 商品カタログ > 税率 > 税率 ID を作成 (txr_からはじまるID)
STRIPE_TAX_RATES=""

# CSP設定
# 検知のみモード `CSP_REPORT_ONLY=true`
# ブロックモード `CSP_REPORT_ONLY=false`
# 確認方法
# ブラウザの開発者ツール → Network タブでレスポンスヘッダーを確認：
# ブロックモード: `Content-Security-Policy: default-src 'self'; ...`
# 検知のみモード: `Content-Security-Policy-Report-Only: default-src 'self'; ...`
# 本番デプロイ前に `CSP_REPORT_ONLY=true` でテストし、コンソールにエラーが出ないことを確認してから、ブロックモードに切り替えることをおすすめします。
CSP_REPORT_ONLY=true

# セッション有効期間（時間単位、デフォルト: 120時間 = 5日間）
# 最大336時間（2週間）まで指定可能
# SESSION_DURATION_HOURS=120

# CloudFront Distribution ID（記事更新時のキャッシュ Invalidation 用）
# ローカル開発では未設定で可（Invalidation がスキップされる）
# CLOUDFRONT_DISTRIBUTION_ID=""

# CloudFront ドメイン（開発環境のメディア URL 用）
# 開発環境ではアップロード画像を CloudFront 経由で表示するために必要
# 本番環境では同一ドメイン（相対パス）が使われるため不要
# NEXT_PUBLIC_CLOUDFRONT_DOMAIN=""

S3_BUCKET_NAME=""
